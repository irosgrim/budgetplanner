import React from 'react';

function Additem(props) {
    return (
        <div className="fixed-menu-btn" onClick={props.handleshowmodal}>
            +
        </div>
    );
}
export default Additem;
